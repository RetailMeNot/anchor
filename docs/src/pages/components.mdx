import { Link } from 'gatsby';
import { ComponentInfo } from '../components/Utils';

# Components

Below are all of the components available in **Anchor**. Most of these will also need to have
the <Link to="/theme/theme-provider">`ThemeProvider`</Link> and <Link to="/theme/theme-provider#roottheme">`RootTheme`</Link> modules
imported as well; see those sections for more details.

<ComponentInfo mode="all" />