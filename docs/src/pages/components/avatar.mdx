import { ApiTable, ComponentInfo } from '../../components/Utils';

# Avatar

<ComponentInfo title="Avatar" mode="description" />

```tsx live
<section>
    <div>
        <Typography tag="h3">Avatar with a label...</Typography>
        <Avatar label="ps" />
    </div>

    <div>
        <Typography tag="h3">Avatar without a label...</Typography>
        <Avatar />
    </div>

    <div>
        <Typography tag="h3">Avatar with an image...</Typography>
        <Avatar alt="User Avatar" src="https://avatars3.githubusercontent.com/u/24923730?s=460&v=4" />
    </div>
</section>
```

The props `src` and `label` are not intended to be used simultaneously.

<br  />

---

## API

<ApiTable data={[
    {
        property: 'label',
        description: `Replace the default avatar graphic with the first two characters of the
            provided string. Letters will be automatically capitalized.`,
        type: 'string',
    },
    {
        property: 'src',
        description: `Replace the default avatar graphic with an image.`,
        type: 'string',
    },
    {
        property: 'alt',
        description: `Replace the default avatar alt tag when a <pre>src</pre> is provided.`,
        type: 'string',
    },
]} />
