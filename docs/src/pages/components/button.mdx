# Button

#### Usage

Buttons are used to complete an action or make a choice. Some examples include Add, Save, Delete, Sign up. As a general
rule, each page should have one primary button. Any remaining calls-to-action are represented as secondary buttons
(e.g. Cancel). 

#### Primary Button
```tsx
<Component initialState={{ size: 'medium' }}>
  {({ state, setState }) => 
    <div>
      <Button size={state.size} onClick={() => { setState({ size: 'small' }) }}>Button Text</Button>
    </div>
  }
</Component>
```

#### Primary Button
```tsx
class Derp extends React.Component {
    constructor(props) {
        console.log(1);
    }
    
    someFunc() {
        const h = 1 === 1;
        const g = true;
        const t = [ { id:1, name: 'Test', value: true } ];
        
        return h;
    }

    render () {
        return (
            <div>
                1
            </div>
        );
    }
}
```
