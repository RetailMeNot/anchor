import { Link } from 'gatsby';
import {
  ApiTable,
  ComponentInfo,
  FormatTypes,
} from '../../components/Utils';

# Button

<ComponentInfo title="Button" />

*NOTE! The example in the live code editor below is using the default `variant`, `filled`.
Editing this field can cause the live code editor to crash, so examples of those variants are provided
below. Also included is an example of an icon button*

```tsx live
<Component initialState={{ size: 'md' }}>
  {({ state, setState }) =>
    <div>
      <Button
        block={false}
        circular={false}
        colorTheme={colors.savvyCyan}
        disabled={false}
        flip={false}
        forceActive={false}
        forceFocus={false}
        forceHover={false}
        minWidth="20rem"
        onClick={() => setState(({ size }) => ({
          size: size === 'sm' ? 'md' : 'sm'
        }))}
        prefix={<AddEvent />}
        reverse={false}
        size={state.size}
        suffix={false}
      >
        Button Text
      </Button>
    </div>
  }
</Component>
```

### Variants

###### Filled Button
```tsx live hideTitle
<Component initialState={{ size: 'md' }}>
  {({ state, setState }) =>
    <div>
      <Button
        onClick={() => setState(({ size }) => ({
          size: size === 'md' ? 'xs' : 'md'
        }))}
        size={state.size}
        variant="filled"
      >
        I'm size '{state.size}'!
      </Button>
    </div>
  }
</Component>
```

###### Outline Button
```tsx live hideTitle
<Component initialState={{ size: 'md' }}>
  {({ state, setState }) =>
    <div>
      <Button
        onClick={() => setState(({ size }) => ({
          size: size === 'md' ? 'lg' : 'md'
        }))}
        size={state.size}
        variant="outline"
      >
        I'm size '{state.size}'!
      </Button>
    </div>
  }
</Component>
```

###### Minimal Button
```tsx live hideTitle
<Component initialState={{ size: 'md' }}>
  {({ state, setState }) =>
    <div>
      <Button
        onClick={() => setState(({ size }) => ({
          size: size === 'md' ? 'sm' : 'md'
        }))}
        size={state.size}
        variant="minimal"
      >
        I'm size '{state.size}'!
      </Button>
    </div>
  }
</Component>
```

###### Icon Button
```tsx live hideTitle
<Component initialState={{ size: 'md' }}>
  {({ state, setState }) =>
    <div>
      <Button
        circular={true}
        onClick={() => setState(({ size }) => ({
          size: size === 'sm' ? 'md' : 'sm'
        }))}
        prefix={<Heart />}
        size={state.size}
      />
    </div>
  }
</Component>
```

---

## API

<ApiTable data={[
    {
        property: 'block',
        description: `If set to true, the <pre>Button</pre> will fill up 100% of available horiztonal
            space.`,
        type: 'boolean',
    },
    {
        property: 'circular',
        description: `If set to true, the <pre>Button</pre>'s corners will be curved to a pill-like
            shape. If <pre>iconOnly</pre> is set to <pre>true</pre>, the button will be a circle.`,
        type: 'boolean',
    },
    {
        property: 'colorTheme',
        description: `Uses the specified color from <strong>Anchor</strong>'s theme to specify colors
            to be used for the <pre>Button</pre>.`,
        type: <Link to="/theme/theme-provider#colors">colors</Link>,
    },
    {
        property: 'disabled',
        description: `If set to true, disables the <pre>Button</pre> preventing the user from
            interacting with it.`,
        type: 'boolean',
    },
    {
        property: 'flip',
        description: `If set to true, makes the upper-right corner of the <pre>Button</pre> look as
            if it is flipped over. Note that this does not work correctly if <pre>circular</pre> is
            set to <pre>true</pre>.`,
        type: 'boolean',
        default: 'false',
    },
    {
        property: 'forceActive',
        description: `If set to true, forces the <pre>Button</pre> to its active state. This is
            generally only used for debugging.`,
        type: 'boolean',
    },
    {
        property: 'forceFocus',
        description: `If set to true, forces the <pre>Button</pre> to appear in its state as if it
            had focus. This is generally only used for debugging.`,
        type: 'boolean',
    },
    {
        property: 'forceHover',
        description: `If set to true, forces the <pre>Button</pre> to appear in its state as if it
            was being hovered. This is generally only used for debugging.`,
        type: 'boolean',
    },
    {
        property: 'minWidth',
        description: `Sets a minimum width for the <pre>Button</pre>. The value can be in any valid
            css unit, <pre>px</pre>, <pre>rem</pre>, <pre>%</pre>, etc. This prop will be ignored if
            <pre>block</pre> is set to true, or if the <pre>Button</pre> has no text and has a
            <pre>prefix</pre> or <pre>suffix</pre> specified.`,
        type: 'string',
    },
    {
        property: 'onClick',
        description: `The function to execute when the button is clicked.`,
        type: '() => void',
    },
    {
        property: 'prefix',
        description: [
            `Although this can be of any value it makes the most sense to pass a component, such as an `,
            <Link to="/components/icon">Icon</Link>,
            ` from <strong>Anchor</strong>. This will place the passed element in front of the
            <pre>Button</pre>'s text. Setting this prop will set the icon to be used for an
            icon-only button when no text is specified.`
        ],
        type: 'any',
    },
    {
        property: 'revealed',
        description: `-`,
        type: 'boolean',
    },
    {
        property: 'reverse',
        description: `Uses the reverse color scheme noted in <pre>colorTheme</pre>.`,
        type: 'boolean',
    },
    {
        property: 'size',
        description: `The different sizes of the <pre>Button</pre>.`,
        type: <FormatTypes data={['lg', 'md', 'sm', 'xs']} />,
        default: 'md',
    },
    {
        property: 'suffix',
        description: [
            `Although this can be of any value it makes the most sense to pass a component,
            such as an `,
            <Link to="/components/icon">Icon</Link>,
            ` from <strong>Anchor</strong>. This will place the passed element after the
            <pre>Button</pre>'s text. Setting this prop will set the icon to be used for an
            icon-only button when no text is specified.`,
        ],
        type: 'any',
    },
    {
        property: 'variant',
        description: `The <pre>Button</pre> has several variants, each of which applies new colors
            based on the selected <pre>colorTheme</pre>. <pre>filled</pre> will fill in the
            background, <pre>outline</pre> will only outline the button, and <pre>minimal</pre> will
            neither have a background or outline.`,
        type: <FormatTypes data={[ 'filled', 'outline', 'minimal']} />,
        default: 'filled',
    },
]} />