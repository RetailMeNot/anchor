import { Link } from 'gatsby';
import {
    ApiTable,
    ComponentInfo,
    FormatTypes
} from '../../components/Utils';

# Collapse

<ComponentInfo title="Collapse" mode="description" />

When using the **Live Code Editor**, you can use any <Link to="/components/icon">Icon</Link> component
available in **Anchor** for the `openedIcon` and `closedIcon` props.

```tsx live
<Collapse
    closedIcon={<ArrowForward />}
    closedText={'Open Me'}
    hasBottomBorder={true}
    isOpen={false}
    openedIcon={<ArrowBack />}
    openedText={'Close Me'}
    variant={'comfortable'}
>
    <div>Hello world.</div>
</Collapse>
```

## CollapseGroup

The **`CollapseGroup`** component combines multiple **`Collapse`** components together, allowing a single component to apply props to all children, unifying their appearance, and adding the ability to have accordion functionality.

```tsx live
<CollapseGroup
    accordion={true}
    closedIcon={<ArrowForward />}
    openedIcon={<ArrowBack />}
    openIndex={1}
    variant="compact"
>
    <Collapse>
        <Typography pl="5">
            I will appear and disappear when the rendered button is clicked!
        </Typography>
    </Collapse>
    <Collapse>
        <Typography pl="5">
            I will also appear and disappear when the rendered button is clicked!
        </Typography>
    </Collapse>
</CollapseGroup>
```

## API's

---

### Collapse

<ApiTable data={[
    {
        property: 'closedIcon',
        description: 'The icon that appears when the component is closed.',
        type: 'JSX',
        default: 'ChevronUp from <strong>Anchor</strong>',
    },
    {
        property: 'closedText',
        description: 'The text that appears on the button as a CTA when the component is closed.',
        type: 'string',
        default: 'Open',
    },
    {
        property: 'hasBottomBorder',
        description: 'Shows/hides the bottom border.',
        type: 'boolean',
        default: 'true',
    },
    {
        property: 'isOpen',
        description: `Determines whether the <pre>Collapse</pre> component is open or not. Changing
            the value of this prop will programatticaly open/close the component, thusly not
            requiring a click event.`,
        type: 'boolean',
        default: 'false',
    },
    {
        property: 'openedIcon',
        description: 'The icon that appears when the component is opened.',
        type: 'JSX',
        default: 'ChevronDown from <strong>Anchor</strong>',
    },
    {
        property: 'openedText',
        description: 'The text that appears on the button as a CTA when the component is opened.',
        type: 'string',
        default: 'Close',
    },
    {
        property: 'variant',
        description: `Several variants of collapse. <pre>comfortable</pre> has considerable spacing,
            <pre>compact</pre> has much tighter spacing, and <pre>none</pre> removes all styling.`,
        type: <FormatTypes types={['comfortable', 'compact', 'none']} />,
        default: 'comfortable',
    },
]} />

---

### CollapseGroup

<ApiTable data={[
    {
        property: 'accordion',
        description: `Enables accordion functionality, where only one <pre>Collapse</pre> component
            can be open at a time.`,
        type: 'boolean',
        default: 'false',
    },
    {
        property: 'closedIcon',
        description: `Using this prop will apply the setting to all child <pre>Collapse</pre>
            components. Otherwise its functionality is as noted above for <pre>Collapse</pre>.`,
        type: 'JSX',
    },
    {
        property: 'openedIcon',
        description: `Using this prop will apply the setting to all child <pre>Collapse</pre>
            components. Otherwise its functionality is as noted above for <pre>Collapse</pre>.`,
        type: 'JSX',
    },
    {
        property: 'openIndex',
        description: `If <pre>accordion</pre> is set to true, this determines which
            <pre>Collapse</pre> component will start in the open state based on index.`,
        type: 'number',
        default: '0',
    },
    {
        property: 'variant',
        description: `Using this prop will apply the setting to all child <pre>Collapse</pre>
            components. Otherwise its functionality is as noted above for <pre>Collapse</pre>.`,
        type: <FormatTypes types={['comfortable', 'compact', 'none']} />,
    },
]} />

