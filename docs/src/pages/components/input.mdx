import { Link } from 'gatsby';
import {
    ApiTable,
    ComponentInfo,
    FormatTypes
} from '../../components/Utils';

# Input

<ComponentInfo title="Input" />

**For more information on theming `Input`, refer to its section
in <Link to="/theme/root-theme#inputs">`RootTheme`</Link>.**

```tsx live
    <Input
        label="Example Label"
        name="example-input"
        placeholder="Please Enter Text..."
        prefix={<ChevronRight />}
        size="md"
        suffix={<Question />}
        type="text"
    />
```

---

### Event Handlers

`Input` has its own methods tied to the various event handlers, `onBlur`, `onChange`, `onFocus`,
`onKeyDown`, and `onKeyUp`. This is intentional, as without these methods the generated `input`
element becomes an <a href="https://reactjs.org/docs/uncontrolled-components.html" target="_blank">uncontrolled component</a>.

The below example shows a way in which you can attach your own methods to these handlers.

```tsx live
<Component initialState={ {value: ''} }>
    {({ state, setState }) =>
        <Input
            label="Example Label"
            name="example-input"
            placeholder="Please Enter Text..."
            prefix={<ChevronRight />}
            size="md"
            suffix={<Question />}
            type="text"
            value={state.value}
            onChange={ newVal => setState(newVal) }
            onBlur={ newVal => console.log(newVal) }
        />
    }
</Component>
```

---

## API

<ApiTable data={[
    {
        property: 'ariaLabel',
        description: `Setting this prop will add the <pre>aria-label</pre> attribute to
            the <pre>Input</pre> and assign the passed value to it.`,
        type: 'string',
    },
    {
        property: 'id',
        description: `Sets the unique <pre>id</pre> attribute for the <pre>Input</pre>.`,
        type: 'string',
    },
    {
        property: 'label',
        description: `Adds a <pre>label</pre> element inside the <pre>Input</pre>. When
            the <pre>Input</pre> receives focus, the label will shift upwards and reduce in
            size. Assigning a <pre>name</pre> will associate the <pre>label</pre> to
            that <pre>name</pre>.`,
        type: 'string',
    },
    {
        property: 'name',
        description: `Adds a <pre>name</pre> attribute to the rendered <pre>Input</pre>.`,
        type: 'string',
        default: 'input',
    },
    {
        property: 'onBlur',
        description: `An event handler for when the <pre>Input</pre> loses focus.`,
        type: '(value?: string | number | any, event?: KeyboardEvent) => any',
    },
    {
        property: 'onChange',
        description: `An event handler for when the content of the <pre>Input</pre> changes.`,
        type: '(value?: string | number | any, event?: KeyboardEvent) => any',
    },
    {
        property: 'onFocus',
        description: `An event handler for when the <pre>Input</pre> gains focus.`,
        type: '(value?: string | number | any, event?: KeyboardEvent) => any',
    },
    {
        property: 'onKeyDown',
        description: `An event handler for when a key is pressed while the <pre>Input</pre> has
            focus.`,
        type: '(value?: string | number | any, event?: KeyboardEvent) => any',
    },
    {
        property: 'onKeyUp',
        description: `An event handler for when a key is released while the <pre>Input</pre> has
            focus.`,
        type: '(value?: string | number | any, event?: KeyboardEvent) => any',
    },
    {
        property: 'placeholder',
        description: `Placeholder text that will be removed once the user beings typing into the
            <pre>Input</pre>. Note that <pre>label</pre> takes precedence. The
            <pre>placeholder</pre> text will appear once the <pre>Input</pre> receives focus if the
            <pre>label</pre> is also set.`,
        type: 'string',
    },
    {
        property: 'prefix',
        description: [
            `Although this can be of any value it makes the most sense to pass a component, such
            as an `,
            <Link to="/components/icon">Icon</Link>,
            ` from <strong>Anchor</strong>. This will place the passed element within the
            <pre>Input</pre> on the left.`
        ],
        type: 'any',
    },
    {
        property: 'size',
        description: `The size of the <pre>Input</pre> field.`,
        type: <FormatTypes data={['sm', 'md', 'lg']} />,
        default: 'md',
    },
    {
        property: 'suffix',
        description: [
            `Although this can be of any value it makes the most sense to pass a component, such
            as an `,
            <Link to="/components/icon">Icon</Link>,
            ` from <strong>Anchor</strong>. This will place the passed element within the
            <pre>Input</pre> on the right.`
        ],
        type: 'any',
    },
    {
        property: 'type',
        description: `What <pre>type</pre> the <pre>Input</pre> component should render as.`,
        type: <FormatTypes data={['email', 'number', 'tel', 'text']} />,
        default: 'text',
    },
    {
        property: 'value',
        description: `The <pre>value</pre> of the rendered <pre>Input</pre>.`,
        type: <FormatTypes data={['string', 'number', 'any']} noInterpret />,
    },
]} />
