import { Link } from 'gatsby';
import {
    ApiTable,
    ComponentInfo,
    FormatTypes
} from '../../components/Utils';

# Typography

<ComponentInfo title="Typography" />

One of the principal features of this component is the concept of **`scale`**.
Although it's possible to alter the `line-height` (`lineHeight` prop) and `font-size` (`size` prop)
of `Typography`, it's recommended to alter its `scale` instead. Often, semantic HTML tagging
doesn't always get the same text styles and this helps alleviate that problem. It also assists in
rendering common fonts regardless of their enclosing tags.

Using `Typography` without modifying any of its props will wrap any text element in a `span` tag.
These are the default styles for Typography:

* `font-family: Avenir Next, Segoe UI, Helvetica Neue, Helvetica, Roboto,sans-serif;`
* `font-size: 1rem;`
* `line-height: 1.5rem;`
* `text-align: inherit;`
* `text-transform: none;`
* `color: inherit;`

```tsx live
<div>
    <Typography tag="p">
        The quick <Typography tag="span" transform="uppercase" color="#ff0000" weight="bolder">red</Typography> fox
    </Typography>
    <Typography tag="h2" color="text.placeholder">
        jumped
    </Typography>
    <Typography tag="p">
        over the <Typography tag="span" scale="28" color="brown">brown</Typography> lazy dog.
    </Typography>
</div>
```

---

## API

<ApiTable data={[
    {
        property: 'align',
        description: `Changes the alignment of text within the component. As this is the application
            of a CSS property, standard CSS rules apply.`,
        type: <FormatTypes data={['center', 'left', 'right', 'inherit', 'justify']} />,
        default: 'inherit',
    },
    {
        property: 'color',
        description: [
            `Sets the font color. Accepts any valid CSS color value. Will use values present in the colors
            <Link to="/theme/colors/">theme</Link>.`
        ],
        type: <FormatTypes data={['string', 'rgb', 'rgba']} noInterpret />,
        default: 'inherit',
    },
    {
        property: 'display',
        description: `Controls the CSS <pre>display</pre> property of the tag.`,
        type: <FormatTypes data={['inline', 'block', 'contents', 'flex', 'grid', 'inline-block',
                    'inline-flex', 'inline-grid', 'inline-table', 'list-item', 'run-in', 'table',
                    'table-caption', 'table-column-group', 'table-header-group',
                    'table-footer-group', 'table-row-group', 'table-cell', 'table-column',
                    'table-row', 'none', 'initial', 'inherit']}
                />,
    },
    {
        property: 'href',
        description: `When used in conjunction with setting the <pre>tag</pre> prop to <pre>a</pre>,
            creates a link to the specified page/resource.`,
        type: <FormatTypes data={['center', 'left', 'right', 'inherit', 'justify']} />,
        default: 'string',
    },
    {
        property: 'htmlFor',
        description: `When used in conjuction setting the <pre>tag</pre> prop to <pre>label</pre>,
            adds the
            <pre><a href="https://www.w3schools.com/tags/att_for.asp" target="_blank">for</a></pre>
            html attribute in order to associate the label to a form element.`,
        type: 'string',
    },
    {
        property: 'lineHeight',
        description: `Changes the <pre>line-height</pre> CSS property. It is not recommended to edit
            this value, as the <pre>scale</pre> prop will adjust both <pre>size</pre> and
            <pre>lineHeight</pre> accordingly. This value is in <pre>px</pre>.`,
        type: 'number',
    },
    {
        property: 'scale',
        description: `This value directly affects the <pre>size</pre> and <pre>lineHeight</pre>
            props directly, ensuring that there is no overlap of text between lines no matter what
            value is input. This value is in <pre>px</pre>.`,
        type: <FormatTypes data={[62 , 52 , 44 , 36 , 32 , 28 , 24 , 20 , 18 , 16 , 14 , 12]} />,
        default: '16',
    },
    {
        property: 'size',
        description: `Changes the <pre>font-size</pre> CSS property. It is not recommended to edit
            this value, as the scale prop will adjust both <pre>size</pre> and <pre>lineHeight</pre>
            accordingly. This value is in <pre>px</pre>.`,
        type: '-',
        default: '16',
    },
    {
        property: 'tag',
        description: `Changing this prop will let the component render as the specified html tag.`,
        type: <FormatTypes data={['a', 'p', 'span', 'h1', 'h2', 'h3', 'h4', 'h5',
                    'h6', 'blockquote', 'address', 'code', 'label', 'pre']}
                />,
        default: 'span',
    },
    {
        property: 'transform',
        description: `Changes the <pre>text-transform</pre> CSS property.`,
        type: <FormatTypes data={['none', 'capitalize', 'uppercase', 'lowercase',
                    'initial', 'inherit']}
                />,
        default: 'none',
    },
    {
        property: 'weight',
        description: `Changes the <pre>font-weight</pre> CSS property.`,
        type: <FormatTypes data={[
                    'normal' , 'bold' , 'bolder' , 'lighter' , '100' , '200' , '300' ,
                    '400' , '500' , '600' , '700' , '800' , '900' , 'initial' , 'inherit'
                ]} />,
    },
]} />
